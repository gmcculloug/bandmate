<h2><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: -3px; margin-right: 8px;"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path></svg>Edit Venue: <%= @venue.name %></h2>

<%= erb :_errors %>

<form method="POST" action="/venues/<%= @venue.id %>" class="card">
    <input type="hidden" name="_method" value="PUT">
    
    <div class="form-group">
        <label for="venue_name">Venue Name *</label>
        <input type="text" id="venue_name" name="venue[name]" value="<%= @venue.name %>" required autofocus>
    </div>

    <div class="form-group">
        <label for="venue_location">Location *</label>
        <input type="text" id="venue_location" name="venue[location]" value="<%= @venue.location %>" placeholder="e.g., 123 Main St, City, State" required>
    </div>

    <div class="form-group">
        <label for="venue_contact_name">Contact Name *</label>
        <input type="text" id="venue_contact_name" name="venue[contact_name]" value="<%= @venue.contact_name %>" placeholder="e.g., John Smith" required>
    </div>

    <div class="form-group">
        <label for="venue_phone_number">Phone Number *</label>
        <input type="tel" id="venue_phone_number" name="venue[phone_number]" value="<%= @venue.phone_number %>" placeholder="e.g., (555) 123-4567" required>
    </div>

    <div class="form-group">
        <label for="venue_website">Website</label>
        <input type="url" id="venue_website" name="venue[website]" value="<%= @venue.website %>" placeholder="https://www.venuewebsite.com">
    </div>

    <div class="form-group">
        <label for="venue_notes">Notes</label>
        <textarea id="venue_notes" name="venue[notes]" rows="4" placeholder="Any additional notes about the venue (capacity, setup requirements, parking info, etc.)"><%= @venue.notes %></textarea>
    </div>

    <%= erb :_form_actions, locals: { submit_text: 'Update Venue', cancel_url: "/venues/#{@venue.id}" } %>
</form> 