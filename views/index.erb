<h2>Welcome to Bandage</h2>
<p>Your complete song and set list management system for bands.</p>

<div style="display: flex; flex-direction: column; gap: 20px; margin-top: 20px;">

    <div class="card">
        <h3>📋 Set Lists</h3>
        <% if @set_lists.any? %>
            <% @set_lists.limit(5).each do |set_list| %>
                <div style="display: flex; justify-content: space-between; align-items: center; padding: 8px 0; border-bottom: 1px solid #e2e8f0; cursor: pointer;" onclick="window.location.href='/set_lists/<%= set_list.id %>'">
                    <div style="flex: 1; min-width: 0;">
                        <div style="font-weight: 600; color: #2d3748; word-break: break-word;"><%= set_list.name %></div>
                        <div style="font-size: 0.85rem; color: #718096;">
                            <span style="background: #667eea; color: white; padding: 2px 6px; border-radius: 10px;">
                                <%= set_list.songs.count %> songs
                            </span>
                        </div>
                    </div>
                    <div class="actions" onclick="event.stopPropagation();" style="flex-shrink: 0;">
                        <a href="/set_lists/<%= set_list.id %>/print" class="btn" style="padding: 4px 8px; font-size: 0.75rem;" target="_blank">Print</a>
                    </div>
                </div>
            <% end %>
            <div style="text-align: center; margin-top: 20px;">
                <a href="/set_lists" class="btn">View All Set Lists</a>
            </div>
        <% else %>
            <p>No set lists created yet. <a href="/set_lists/new" class="btn">Create your first set list!</a></p>
        <% end %>
    </div>

    <div class="card">
        <h3>🎵 Recent Songs</h3>
        <% if @songs.any? %>
            <% @songs.limit(3).each do |song| %>
                <div style="display: flex; justify-content: space-between; align-items: center; padding: 8px 0; border-bottom: 1px solid #e2e8f0; cursor: pointer;" onclick="window.location.href='/songs/<%= song.id %>'">
                    <div style="flex: 1; min-width: 0;">
                        <div style="font-weight: 600; color: #2d3748; word-break: break-word;"><%= song.title %></div>
                        <div style="font-size: 0.85rem; color: #718096; display: flex; gap: 8px; flex-wrap: wrap;">
                            <span><%= song.artist %></span>
                            <span style="background: #667eea; color: white; padding: 2px 6px; border-radius: 10px;">
                                <%= song.key %>
                            </span>
                            <% if song.tempo %>
                                <span style="background: #38a169; color: white; padding: 2px 6px; border-radius: 10px;">
                                    <%= song.tempo %> BPM
                                </span>
                            <% end %>
                        </div>
                    </div>
                    <div class="actions" onclick="event.stopPropagation();" style="flex-shrink: 0;">
                        <a href="/songs/<%= song.id %>/edit" class="btn" style="padding: 4px 8px; font-size: 0.75rem;">Edit</a>
                    </div>
                </div>
            <% end %>
            <div style="text-align: center; margin-top: 20px;">
                <a href="/songs" class="btn">View All Songs</a>
            </div>
        <% else %>
            <p>No songs added yet. <a href="/songs/new" class="btn">Add your first song!</a></p>
        <% end %>
    </div>
</div>

 