# Bandmate Docker Configuration
# Copy this file to .env and update with your settings

# Account creation secret for new user registration
BANDMATE_ACCT_CREATION_SECRET=your_secret_key_here

# External PostgreSQL Database Configuration (for macOS host)
# Use host.docker.internal to connect to PostgreSQL running on macOS host
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=bandmate_production
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=bandmate_password

# Alternative: Use DATABASE_URL instead of individual settings
# DATABASE_URL=postgresql://username:password@host:port/database_name

# Valkey Session Store (Docker environments only)
# Enables shared sessions across multiple app instances for horizontal scaling
# Leave VALKEY_ENABLED unset for native development/test (uses cookie sessions)
VALKEY_ENABLED=
VALKEY_HOST=valkey
VALKEY_PORT=6379
VALKEY_DB=0
VALKEY_POOL_SIZE=5

# Optional: Session secret (will use default if not set)
# Generate with "openssl rand -hex 64" for Production usage
# SESSION_SECRET=your_very_long_and_secure_session_secret_at_least_64_characters_long

# Google Calendar Integration (optional)
# For Google Calendar integration, create a service account in Google Cloud Console
# and download the JSON credentials. Set the entire JSON as this environment variable.
# See GOOGLE_CALENDAR_SETUP.md for detailed setup instructions.
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}
